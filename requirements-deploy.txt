# Minimal requirements for deployment (CPU-only)
# Install torch separately with CPU-only wheel to save ~1.5GB

--extra-index-url https://download.pytorch.org/whl/cpu

# Core
torch==2.1.0+cpu
numpy>=1.24.0,<2.0
pandas>=2.0.0

# API clients
requests>=2.31.0

# ML utilities
scikit-learn>=1.3.0
joblib>=1.3.0

# Scheduling
schedule>=1.2.0
APScheduler>=3.10.0

# Logging
python-json-logger>=2.0.0

# Optional: if you want to expose a simple API
# fastapi>=0.100.0
# uvicorn>=0.23.0
